# qnsz-website 项目技术架构文档

> 全能数字官网 - 用科技打破空间界限  
> 文档版本：1.0 | 更新日期：2025-03-06

---

## 一、项目概述

### 1.1 项目简介

qnsz-website 是全能数字（全能数字）企业官网，采用 Next.js 16 App Router 构建，支持静态导出部署。网站涵盖产品介绍、平台解决方案、下载中心、关于我们等核心模块，面向远程协作、音视频通信、应急指挥等业务场景。

### 1.2 技术栈总览

| 类别 | 技术选型 | 版本 | 说明 |
|------|----------|------|------|
| 框架 | Next.js | 16.1.6 | App Router、静态导出 |
| UI 库 | React | 19.2.3 | 服务端/客户端组件 |
| 语言 | TypeScript | ^5 | 严格模式 |
| 样式 | Tailwind CSS | ^4 | PostCSS 集成 |
| 动画 | framer-motion | ^11.15.0 | 页面与交互动画 |
| HTTP | axios | ^1.13.6 | API 请求封装 |
| 二维码 | qrcode.react | ^4.2.0 | 下载页二维码展示 |

---

## 二、项目目录结构

```
qnsz-website/
├── .cursor/                    # Cursor AI 技能与配置
│   └── skills/
│       └── generate-website/   # 网站生成技能集
├── public/                     # 静态资源（根路径 /）
│   ├── file.svg
│   ├── vercel.svg
│   ├── window.svg
│   └── images/                 # 图片资源（logo、产品图等）
├── src/
│   ├── app/                    # App Router 路由与页面
│   │   ├── layout.tsx          # 根布局
│   │   ├── page.tsx            # 首页
│   │   ├── globals.css         # 全局样式
│   │   ├── favicon.ico
│   │   ├── sitemap.ts          # 站点地图生成
│   │   ├── robots.ts           # robots.txt 生成
│   │   ├── about/page.tsx      # 关于我们
│   │   ├── download/page.tsx   # 下载中心
│   │   └── platform/           # 平台解决方案
│   │       ├── fusion/page.tsx  # 融合通信
│   │       ├── meeting/page.tsx # 视频会议
│   │       ├── talkback/page.tsx # 数字对讲
│   │       └── emc/page.tsx    # 应急指挥
│   ├── components/
│   │   ├── layout/             # 布局组件
│   │   │   ├── Header.tsx
│   │   │   └── Footer.tsx
│   │   ├── ui/                 # 通用 UI 组件
│   │   │   ├── ContactModal.tsx
│   │   │   ├── SliderCaptcha.tsx
│   │   │   └── ImageWithFallback.tsx
│   │   └── sections/           # 页面区块组件
│   │       ├── HeroCarousel.tsx
│   │       ├── CoreValue.tsx
│   │       ├── ProductCapabilities.tsx
│   │       ├── SolutionCTA.tsx
│   │       ├── about/          # 关于页区块
│   │       ├── download/       # 下载页区块
│   │       ├── emc/            # 应急指挥区块
│   │       ├── fusion/         # 融合通信区块
│   │       ├── meeting/        # 视频会议区块
│   │       └── talkback/       # 数字对讲区块
│   ├── hooks/
│   │   └── useDeviceType.ts    # 设备类型检测
│   └── lib/
│       ├── service.ts          # HTTP 服务封装
│       ├── site.ts             # 站点配置
│       └── motion-variants.ts  # 动画变体定义
├── next.config.ts
├── tsconfig.json
├── postcss.config.mjs
├── package.json
└── README.md
```

---

## 三、路由与页面架构

### 3.1 路由映射表

| 路由 | 文件路径 | 页面说明 |
|------|----------|----------|
| `/` | `src/app/page.tsx` | 首页 - 轮播、核心价值、产品能力、解决方案 CTA |
| `/about` | `src/app/about/page.tsx` | 关于我们 - 公司介绍、发展历程、资质、联系方式 |
| `/download` | `src/app/download/page.tsx` | 下载中心 - 多平台下载、版本信息、验证码 |
| `/platform/fusion` | `src/app/platform/fusion/page.tsx` | 融合通信解决方案 |
| `/platform/meeting` | `src/app/platform/meeting/page.tsx` | 视频会议解决方案 |
| `/platform/talkback` | `src/app/platform/talkback/page.tsx` | 数字对讲解决方案 |
| `/platform/emc` | `src/app/platform/emc/page.tsx` | 应急指挥解决方案 |

### 3.2 布局结构

```
RootLayout (layout.tsx)
├── Header（固定顶栏、平台导航、移动端菜单）
├── {children}（页面内容）
└── Footer（链接、版权、备案、服务协议）
```

- **Header**：平台下拉菜单、移动端汉堡菜单、响应式导航
- **Footer**：多列链接、企业微信二维码、备案信息、服务协议与隐私政策

---

## 四、组件架构

### 4.1 组件分类

| 分类 | 路径 | 职责 |
|------|------|------|
| 布局 | `components/layout/` | 全局 Header、Footer |
| 通用 UI | `components/ui/` | 弹窗、验证码、图片降级等 |
| 区块 | `components/sections/` | 按业务域拆分的页面区块 |

### 4.2 核心组件说明

| 组件 | 功能 |
|------|------|
| `Header` | 顶栏导航、平台下拉、移动端菜单 |
| `Footer` | 页脚链接、版权、备案、服务协议 |
| `ContactModal` | 企业微信二维码弹窗 |
| `SliderCaptcha` | 滑块验证码（framer-motion 动画） |
| `ImageWithFallback` | 图片加载失败时降级展示 |
| `HeroCarousel` | 首页轮播 |
| `CoreValue` | 核心价值展示 |
| `ProductCapabilities` | 产品能力展示 |
| `SolutionCTA` | 解决方案行动号召 |

### 4.3 区块组件组织

区块按业务域拆分，便于维护与复用：

- **about/**：CompanyIntro、Timeline、Certifications、ContactInfo、JoinUs、AboutBanner
- **download/**：DownloadHero、DownloadCards、ProductFeatures
- **fusion/**：FusionBanner、FusionFeatures、FusionScenarios、FusionCapabilities、FusionMultiTerminal
- **meeting/**：MeetingBanner、MeetingFeatures、MeetingArchitecture、MeetingScenarios、MeetingProducts
- **talkback/**：TalkbackBanner、TalkbackFeatures、TalkbackAI、TalkbackScenarios、TalkbackProducts、TalkbackDataSecurity
- **emc/**：EmcBanner、EmcAnalysis、EmcScenarios、EmcProcess、EmcSmartPlan

多数区块为 `"use client"` 组件，用于动画、交互及 API 调用。

---

## 五、数据层与服务层

### 5.1 HTTP 服务封装（`src/lib/service.ts`）

基于 axios 的 HTTP 客户端：

```typescript
// 方法
get(url, params)   // GET 请求
post(url, data)    // POST 请求
put(url, data)     // PUT 请求
patch(url, data)   // PATCH 请求
del(url)           // DELETE 请求
```

**配置：**

- **baseURL**：开发 `https://172.25.11.120`，生产 `https://www.onlineinline.cn`
- **timeout**：15000ms
- **withCredentials**：true
- **Content-Type**：application/json
- 统一错误处理与日志输出

### 5.2 站点配置（`src/lib/site.ts`）

- `SITE_URL`：站点 URL，支持 `NEXT_PUBLIC_SITE_URL` 环境变量
- 默认值：`https://www.onlineinline.com`

### 5.3 动画变体（`src/lib/motion-variants.ts`）

集中管理 framer-motion 动画配置：

- 缓动与时长：EASE、SPRING、DURATION、OFFSET、STAGGER
- 视口配置：defaultViewport、heroViewport 等
- Variants：heading、paragraph、badge、image、card、modal、交互状态等

### 5.4 自定义 Hooks

**useDeviceType**（`src/hooks/useDeviceType.ts`）

- 返回值：`android` | `ios` | `desktop`
- 用途：下载页按设备展示对应平台下载入口

---

## 六、API 与数据请求

### 6.1 API 代理配置

项目未使用 `src/app/api/` 下的 Next.js API Routes，而是通过 `next.config.ts` 的 `rewrites` 将 `/api/*` 代理到外部服务：

```typescript
// next.config.ts
async rewrites() {
  return [
    {
      source: "/api/:path*",
      destination: "http://172.25.11.120/:path*",
    },
  ];
}
```

- **开发环境**：请求经 Next.js 代理到 `http://172.25.11.120`
- **生产环境**：`service.ts` 直接请求 `https://www.onlineinline.cn`

### 6.2 使用的接口

| 接口路径 | 方法 | 用途 |
|----------|------|------|
| `/api/v1/auth/connect/test` | POST | Footer 获取服务器时间 |
| `/api/voip/v1/sliderCaptcha/generate` | GET | 滑块验证码获取 |
| `/api/voip/v1/sliderCaptcha/verify` | POST | 滑块验证码校验 |
| `/api/voip/v1/sliderCaptcha/getSign` | GET | 获取签名（下载二维码） |
| `/api/voip/v1/upgrade/officialWebsite/latest/info` | GET | 各平台最新版本信息 |

---

## 七、样式与资源

### 7.1 样式方案

- **框架**：Tailwind CSS v4 + `@tailwindcss/postcss`
- **全局样式**：`src/app/globals.css`
- **导入方式**：`@import "tailwindcss"`
- **字体**：Geist Sans、Geist Mono（next/font/google）

### 7.2 CSS 变量

```css
--background
--foreground
--font-geist-sans
--font-geist-mono
```

支持 `prefers-color-scheme: dark` 与 `prefers-reduced-motion`。

### 7.3 自定义动画

- **keyframes**：`fade-in-up`、`marquee`
- **工具类**：`.animate-marquee`、`.scrollbar-hide`

### 7.4 静态资源

- **根目录**：`public/`
- **图片**：`public/images/`，代码中引用为 `/images/xxx`
- **常见路径**：`/images/logo.png`、`/images/enheylig.png`、`/images/qywx-qrcode.png`、`/images/emc/`、`/images/talkback/`、`/images/meeting/`、`/images/fusion/` 等

---

## 八、构建与部署

### 8.1 构建配置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| output | `"export"`（生产） | 静态导出 |
| distDir | `"dist"` | 构建输出目录 |
| trailingSlash | false | 不自动添加尾部斜杠 |

### 8.2 脚本命令

```bash
npm run dev    # 开发服务器
npm run build  # 生产构建（生成 dist/）
npm run start  # 生产模式启动（需先 build）
```

### 8.3 部署特性

- **静态站点**：无服务端运行时，适合 CDN、对象存储、静态托管
- **SEO**：`sitemap.ts`、`robots.ts` 自动生成
- **robots.txt**：允许 `/`，禁止 `/api/`、`/_next/`、`/download/`，sitemap 指向 `{SITE_URL}/sitemap.xml`

---

## 九、环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `NEXT_PUBLIC_SITE_URL` | 站点 URL | `https://www.onlineinline.com` |
| `NODE_ENV` | 环境标识 | development / production |

---

## 十、技术要点总结

1. **Next.js 16 App Router**：文件系统路由、布局、元数据
2. **静态导出**：纯静态部署，无 Node 运行时
3. **API 代理**：开发环境通过 rewrites 解决跨域
4. **组件化**：布局 / UI / 区块分层，按业务域组织
5. **动画**：framer-motion + 统一 motion-variants
6. **响应式**：Tailwind + useDeviceType 适配多端
7. **SEO**：metadata、sitemap、robots 配置完整

---

*文档由项目架构分析自动生成，如有变更请及时更新。*
